import SetupTemplate from '../templates/SetupTemplate.template';

export default function SetupPage() {
  return <SetupTemplate headDescription="First-time setup for Binance API credentials." headTitle="Setup | BTC Market Scanner" />;
}
